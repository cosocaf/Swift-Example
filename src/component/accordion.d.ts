import React from "react";
declare type AccordionProps = {
    showMessage: string;
    hideMessage: string;
    children?: React.ReactNode;
};
export declare const Accordion: ({ showMessage, hideMessage, children, }: AccordionProps) => JSX.Element;
export {};
