import React from "react";
declare type PublicBasedLinkProps = {
    children?: React.ReactNode;
    className?: string;
    to: string;
};
export declare const PublicBasedLink: ({ children, className, to, }: PublicBasedLinkProps) => JSX.Element;
export {};
